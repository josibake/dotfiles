syntax enable                   " enable syntax processing
set tabstop=4                   " read tabs as four spaces
set softtabstop=4               " insert tabs as four spaces
set expandtab                   " actually, just treat all tabs as spaces
syntax on                       " turn on syntax highlighting
